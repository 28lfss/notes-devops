# API Base URL
# For local development with Docker Compose (using nginx proxy):
# Leave empty or unset to use default '/api' (proxied via nginx)

# For local development without Docker (direct connection to backend):
# VITE_API_URL="http://localhost:3000/api"

# For production/staging:
# VITE_API_URL="https://api.yourdomain.com/api"
