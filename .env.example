# Docker Compose Environment Variables
# This file is used when running the application with docker-compose

# JWT Secret Key (REQUIRED)
# Generate a strong secret key for production
# You can generate one with: openssl rand -base64 32
JWT_SECRET="your-secret-key-change-in-production"

# Note: DATABASE_URL, PORT, and NODE_ENV are set in docker-compose.dev.yml
# You can override them here if needed by uncommenting:
# DATABASE_URL="postgresql://notes_user:notes_password@postgres:5432/notes_db?schema=public"
# PORT=3000
# NODE_ENV=development
